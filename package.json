{
  "name": "toeic-flashcards-vanilla",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx serve -l tcp://0.0.0.0:3000",
    "build": "mkdir -p dist && cp index.html dist/index.html",
    "preview": "npx serve dist -l tcp://0.0.0.0:3000"
  },
  "dependencies": {},
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
